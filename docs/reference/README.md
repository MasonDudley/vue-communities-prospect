# Reference Notes

These files were copied into the canonical VUE Communities repo to keep the source-backed context in one place.

- `ORIGINAL_SITE_CONFIRMED_FACTS.md` is the safest source of truth for copy updates.
- `CODEX_HANDOFF.md` explains the earlier OpenClaw restore and why the static-style build mattered.
- `extraction-summary.txt` and `live-site-sources.txt` capture the original site review notes.

When copy conflicts across source pages, prefer the confirmed facts file and mark uncertain details for client confirmation instead of guessing.
